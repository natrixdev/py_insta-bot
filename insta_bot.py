from pypage import *
import natrix-insta.py as .

# connfig 
name = "instagram username (@natrix)"
pass = "password"
hash = "#you want to sub"

random = true # dont change this
bOt_not_dect = true # dont chnage this



pypage.newwindow(script=>{
  def _ for x in range(random(1;999):
  .init & .login && .sub && .dislogin
  
  else: 
    destroy() => print("bot was detected, program stopped")
    
    
    .checkIf(Account.Detected) => if yes == yes :
    destroy & print("stopped, account found by intagram anti-bot")
    else => run()












}
